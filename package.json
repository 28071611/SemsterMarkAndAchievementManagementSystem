{
    "name": "edutrack-unified",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install:all": "npm install --prefix backend && npm install --prefix frontend",
        "build:frontend": "npm install --prefix frontend && npm run build --prefix frontend",
        "build:backend": "npm install --prefix backend",
        "build": "npm run build:frontend && npm run build:backend && rm -rf backend/build && cp -r frontend/build backend/build",
        "start": "cd backend && npm start",
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
        "heroku-postbuild": "npm run build"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}